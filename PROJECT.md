# CoinSignal / 코인시그널

개인 관리자용 국내 가상자산 분석 및 모의투자 연구실. 현재 모의 주문만 지원하며 실제 거래 API 호출 코드는 포함하지 않는다.

## 구현 상태
- Owner-private Sites + ChatGPT 로그인. API마다 서버 인증. 가상 계좌는 사용자와 거래소별 분리.
- 업비트/빗썸 v1 공개 시세, 원화 종목 목록, 1시간봉, 호가 REST 어댑터.
- 코인원 public/v2 공개 시세, 차트, 호가 어댑터.
- 완료된 1시간봉 최대 200개로 SMA20/60, 단순 RSI14, 거래량 배수, 시간 수익률 표준편차.
- 규칙 점수는 확률이 아니다. 통계적 학습이나 장기 수익성 검증을 완료하지 않았다.
- 서버 저장 가상 계좌, 거래 기록, 운용 설정, 실행 시점별 평가 자산. SQLite D1 CAS 버전 검사로 동시 요청의 이중 반영 방지.
- 매수는 금액(수수료 포함), 매도는 전량. 호가 단계를 소진하며 별도 슬리피지와 수수료 가정 적용. 부족한 호가에서는 주문 전체 거절. 실제 지연/체결 대기열은 재현하지 못한다.
- 과거 전략 탐색은 다음 1시간봉 종가에 실행한다. 장기 검증 및 실제 호가 기반 검증을 대체하지 않는다.
- 반복 운용은 활성 브라우저에서 65초마다 요청. 백그라운드 24시간 실행은 미연결.
- OpenAI Responses API 해석 엔드포인트 구현. OPENAI_API_KEY 연결 전에는 명시적으로 비활성. 모델은 OPENAI_MODEL 설정 가능.

## 남은 외부 연결 / 검증
- 현재 제작 환경의 외부 네트워크 제한으로 거래소 실 API 응답 확인은 타임아웃. 배포 환경에서 각 연결 상태를 확인해야 한다. 데이터 실패 시 가짜 시세로 대체하지 않는다.
- OpenAI Developers 연결과 서버 비밀 키 설정.
- X/공식 공지 수신 서비스, 최초 수신 시각 보관, 이벤트 정규화/중복 제거는 미구현.
- 장기 과거 데이터 수집, 워크포워드 확률 모델/교정, 코인 동일성의 네트워크·주소 매핑은 후속 단계.
- 상시 실행 서버와 스케줄러는 후속 단계. 사용자가 선택한 로컬 환경에서 실행 프로세스로 구축 가능.

## 향후 실제 거래
실제 주문을 추가할 때 PaperBroker와 별도 LiveBroker 모듈을 구성해야 한다. 거래소별 인증·수량 반올림·최소 주문·체결 조회·멱등성·재시도·잔고 대사·부분 체결·중단 장치를 검증하고 주문 권한을 명시적으로 활성화한다. 현재 UI의 거래소 선택은 실제 계정 연결이 아니다. 출금 권한은 필요하지 않다.

## 로컬 이관
사용자가 추후 위치를 지정할 예정이며 현재 PC의 임의 경로로 내보내지 않는다. 구현은 TypeScript 소스로 유지한다. 현 배포의 D1, Cloudflare 환경, SIWC 인증은 로컬 이관 시 SQLite 저장 어댑터·로컬 관리자 인증·실행 스케줄러로 바꿔야 한다. 로그인 검사를 제거하거나 인증 헤더를 외부에서 신뢰하게 만들지 않는다. 환경 비밀 값은 소스와 별도로 이전한다. 데이터는 계좌/거래소별로 안전하게 내보내고 수량·잔고를 대사한다.

## 환경
.env.example 참조. 비밀 키는 Git에 저장하지 않는다. 스키마는 db/schema.ts, 생성된 마이그레이션은 drizzle/. 코어 계산은 lib/paper.ts, 시장 데이터·지표는 lib/market.ts. 일반 프로젝트 실행은 README의 런타임 안내 참조.
