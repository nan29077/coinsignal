# CoinSignal — 개발 메모

## v2 (2026-09) 요약
- Cloudflare Workers/D1/ChatGPT 로그인(Sites) 구조를 **Node 22 + Hono + SQLite(node:sqlite) + Vite React** 단일 프로세스로 전환. 로컬 PC와 AWS에서 동일하게 실행.
- 규칙 기반 자동 운용(v1 cycle)을 **AI 리서치 기반 분산투자 엔진**으로 대체. 규칙 점수는 스크리닝 입력과 시장·백테스트 화면에서만 사용.
- 계좌: (upbit|bithumb|coinone) × (paper|live). 서로 독립. 실행 방식 auto/approval.

## v1 점검 결과 반영 내역
| v1 문제 | v2 처리 |
|---|---|
| 코인원 소문자 심볼·timestamp NaN | 심볼 대문자 정규화, 티커별 timestamp 사용, 코인원 종목명(currencies) |
| 자동 운용 중복 실행 오판(브라우저 65초 vs 서버 60초) | 서버 스케줄러 + 계좌별 락으로 대체 |
| 손절이 분석 성공에 종속 | 매분 리스크 감시가 시세만으로 손절·익절 실행 |
| 스테이블코인·신규상장 매수 | 스크리닝에서 페깅 자산·경고종목·이력 150봉 미만·거래대금 10억 미만 제외 |
| 한 행 JSON 무한 증가 | 테이블 분리, 자산 기록 5분 단위 14일/1시간 단위 1년, 이벤트 90일 보관 |
| 백테스트 비중 왜곡 | 전략·보유 모두 전액 투입 기준, 비교 곡선 표시 |
| 빗썸 timestamp +9h | 10분 이상 미래이며 9시간 차이인 경우만 보정 |
| OpenAI 700 토큰 | 60k + background 모드 폴링 + 추론 강도 설정 |
| 음수 금액 소수점 | 절댓값 기준 자릿수 |
| 에러 코드 | AppError(status), 거래소 장애 502, 내부 오류 메시지 비노출 |
| 사소 UX 항목 | 차트 날짜 표시, 봉 마감 시각 표기, 상폐·시세 없음 처리 등 |
| 코드 품질 | 전 파일 가독 형식, 타입체크, 단위·통합 테스트 22개 |

## 코드 리뷰(독립 검토) 반영
- 응답 유실·5xx 주문은 `unknown`으로 두고 client order id로 재조회(10분 후 미발견 시 실패)
- 잔고 기준 보유량 축소는 계좌 락 안(감시 루프)에서만, 미체결 주문 있는 종목 제외
- AI 추천이 전부 무효면 결과 폐기, 제외된 비중은 현금으로, AI가 평가하지 않은 보유 코인은 매도하지 않음
- 실매도 수량은 거래소 가용 수량으로 제한, 최소 주문 미만 먼지 잔량은 매도 시도 안 함
- 실패한 손절은 10분 백오프, 미체결 주문 있는 종목은 건너뜀
- 새 계획 생성 시 기존 승인 대기 계획 만료, 승인 후 실행은 미리보기에 있던 주문만(매수 1.5배 한도)
- 긴급 중단·계좌 정지를 주문마다 재확인, 정지/중단 중 수동 매수 차단
- 로그인 제한은 소켓 IP 기준(프록시 신뢰는 옵션) + 전역 제한, 상태 변경 요청은 Origin/Referer 필수
- 재시작 시 실행 중이던 계획 실패 처리, 전송 중 실주문은 unknown으로 재조회

## 검증하지 못한 부분 (실키 필요)
- 빗썸 v2 주문, 코인원 v2.1 주문의 실제 응답 형식은 공식 문서 기준으로 구현, 실계정 소액 테스트 필요
- OpenAI 모델별 실제 비용·소요 시간
- 텔레그램 실제 봇 연동

## 향후 과제
- 지정가/분할 주문, 부분 체결 대기 주문 취소
- 코인원 투자유의 종목 데이터 연동
- 리서치 결과 사후 성과 추적(추천 종목 수익률 기록)
- 다중 사용자/권한 (현재 단일 관리자)
